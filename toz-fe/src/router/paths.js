const transfer = '/transfer'
const home = '/'
const pets = '/pets'
const petDetails = '/petDetails/:id'
const news = '/news/:id'
const linkExpired = '/activation-link-expired'
const firstLogin = '/first-login'

export default { transfer, home, news, pets, petDetails, linkExpired, firstLogin }
